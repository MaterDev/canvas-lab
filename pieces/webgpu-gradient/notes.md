# WebGPU Gradient

Full-screen triangle trick + animated WGSL fragment shader. **View in real Chrome**
(`http://127.0.0.1:4860/`) for GPU rendering — the headless viewer mirror shows black
because it runs `--disable-gpu`. Template for WGSL fragment experiments.
