# WebGPU Raymarch

A stress-test piece: signed-distance raymarching (rotating torus over a ground plane) with soft
shadows and ambient occlusion, up to 90 ray steps per pixel. Used to find the real GPU ceiling of
the readback presentation path on the Adreno 740.
