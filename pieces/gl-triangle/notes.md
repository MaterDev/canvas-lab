# GL Gradient Triangle

Minimal WebGL2 pipeline (VAO-less, single interleaved buffer). Note `destroy()` calls
`WEBGL_lose_context` so switching pieces frees the GPU context. Template for shader work.
